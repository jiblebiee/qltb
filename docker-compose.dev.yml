# Cấu hình PHÁT TRIỂN: mount mã nguồn và bật reload.
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up
services:
  web:
    volumes: ["./:/app"]
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    user: root
